# 🚀 KBO 승패 예측: 영끌(Score Boosting) 방법론 총정리

이 문서는 베이스라인 모델에서부터 최종 **1147점 최고점**을 달성하기까지 도입한 핵심 모델링 기법과 피처 엔지니어링 방법론을 총망라한 문서입니다. 다른 팀원분들의 모델에 아래의 기법들을 이식하여 점수를 극대화하는 데 활용하시기 바랍니다.

---

## 1. Feature Engineering (피처 엔지니어링)

가장 큰 성능 향상을 가져온 부분으로, 단순 통계를 넘어 **야구 도메인 지식**을 수치화하고 모델이 이해하기 쉽게 가공했습니다.

*   **히스토리 갭 (History Gap) 피처**
    *   **개념**: 선수의 "현재 폼(Form)"을 수치화하는 기법.
    *   **구현**: 통산(누적) 타율/방어율과 최근 며칠간의 타율/방어율의 **차이(Gap)**를 계산합니다. 플러스면 폼이 상승세, 마이너스면 하락세임을 모델이 명확하게 인지합니다.
*   **클러치 / 압박감 지수 (Clutch Pressure)**
    *   **개념**: 경기 후반, 점수차가 적을 때 발생하는 특유의 긴장감을 모델링.
    *   **구현**: `(9 - 현재 이닝)`이 적을수록, `abs(홈팀 점수 - 원정팀 점수)`가 적을수록 가중치가 커지는 수식을 적용하여 승패가 뒤집힐 확률이 높은 구간을 특정했습니다.
*   **모멘텀 지수 (Momentum)**
    *   **개념**: 오늘 경기가 투수전 양상인지, 타격전(난타전) 양상인지를 추정.
    *   **구현**: `(양 팀 합산 점수) / (현재 이닝)`을 계산하여 값이 높으면 타자에게 유리한 피처에 가중치를 주고, 낮으면 투수 스탯에 가중치를 주는 비선형 조합(교차 피처)을 생성했습니다.
*   **투수 피로도 누적 지수**
    *   투수의 최근 3경기 투구 수 합산 및 최근 휴식일(일수)을 조합하여 피로도로 인한 제구력 저하 가능성을 반영했습니다.

---

## 2. Advanced Modeling & Ensemble (고급 모델링 기법)

단일 모델의 과적합을 막고, 서로 다른 장점을 가진 모델들을 엮는 구조입니다.

*   **이기종 트리 모델 앙상블 (Heterogeneous Stacking)**
    *   CatBoost (범주형 변수 처리에 강점)
    *   LightGBM (학습 속도가 빠르고 Target Encoding 시 성능 우수)
    *   XGBoost (결측치 처리와 트리의 깊은 규제에 강력함)
    *   **효과**: 동일한 데이터라도 세 모델이 틀리는 구간이 다르기 때문에 예측의 안정성이 크게 올라갑니다.
*   **잔차 보정 (Residual Boosting) 기법** 🔥 *(강력 추천)*
    *   **개념**: Base 모델이 맞추지 못한 오차만 집중적으로 학습하는 보조 모델을 덧붙이는 방식.
    *   **구현 로직**:
        1. 1차 Base 모델(CatBoost 등)로 예측 확률(`pred_1`) 생성
        2. 실제 정답(`y`)과 예측 확률의 차이, 즉 잔차(`Residual = y - pred_1`) 계산
        3. 2차 모델(Regressor)을 학습시켜 이 잔차를 예측하게 함
        4. 최종 예측 = `pred_1 + 예측된 잔차`
*   **Target Encoding (데이터 누수 방지 OOF 적용)**
    *   팀명, 구장명, 선수명 등의 범주형 변수를 타겟(승리 여부)의 평균 확률로 치환(Target Encoding)하되, 과적합(Leakage)을 막기 위해 **반드시 K-Fold의 OOF(Out-Of-Fold) 방식**으로만 인코딩을 적용했습니다.

---

## 3. Hyperparameter Tuning (초매개변수 최적화)

경험에 의존한 파라미터 설정을 배제하고, 알고리즘을 통한 한계치 돌파를 시도했습니다.

*   **Optuna TPE (Tree-structured Parzen Estimator) 최적화**
    *   모델별로 50번 이상의 Trial을 수행하며 베이지안 최적화 기법을 사용해 탐색했습니다.
    *   **주요 탐색 파라미터**:
        *   `learning_rate`: 0.001 ~ 0.1 구간 Log 탐색
        *   `depth` / `num_leaves`: 트리의 복잡도 조절
        *   `reg_lambda` / `l2_leaf_reg`: L2 정규화를 강하게 튜닝하여 과적합 억제
*   **메타 가중치 (Meta-Weight) 최적화**
    *   앙상블 시 단순 평균(0.33 씩)을 내는 대신, 로지스틱 회귀(Logistic Regression)나 Optuna를 활용해 **어떤 모델의 예측값을 더 신뢰할지 가중치를 기계학습으로 도출**했습니다.

---

## 4. Calibration & Leak-Free Validation (확률 보정 및 검증)

대회 평가지표(Dacon Brier Score 등)의 특성에 맞춘 확률 캘리브레이션입니다.

*   **3-Season 시계열 교차 검증 (Leave-One-Season-Out)**
    *   미래의 데이터가 과거 학습에 사용되는 Data Leakage를 막기 위해 무작위 K-Fold 대신 **연도별(2022, 2023, 2024)**로 완벽하게 쪼개서 OOF 검증을 수행했습니다. 이 검증 점수(Validation Score)가 실제 제출 점수와 매우 유사하게 따라갑니다.
*   **Isotonic Regression / Platt Scaling 캘리브레이션** 🔥
    *   **문제점**: 트리 모델들은 이진 분류 시 예측 확률이 0.4 ~ 0.6 등 중간에 몰리거나(Under-confidence), 반대로 0이나 1에 너무 치우치는(Over-confidence) 고질적인 문제(Calibration Error)가 있습니다.
    *   **해결방안**: 훈련된 예측 확률 결과물에 Isotonic Regression 기법을 한 번 더 적용하여, 실제 정답 확률 분포에 맞게 예측값을 부드럽게 펴주었습니다. (Brier Score 평가 방식에서 비약적인 점수 상승 유발)

---

### 💡 팀원 적용 가이드 (우선순위)
팀원분들 모델에 바로 붙였을 때 가성비 좋게 점수가 오를 수 있는 기법의 추천 순위입니다.

1. **(즉시 적용)** 확률 캘리브레이션 (Isotonic Regression) 도입
2. **(쉬운 적용)** 메타 가중치를 통한 앙상블 (단순 앙상블 ➡️ 로지스틱 회귀 스태킹)
3. **(약간의 공수)** 잔차 보정 (Residual Boosting) 구조 설계
4. **(데이터 공수)** 클러치 상황, 모멘텀 지수, History Gap 등 야구 도메인 피처 추가
